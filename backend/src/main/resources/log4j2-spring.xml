<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Properties>
        <Property name="APP_NAME">desafio-hotel</Property>
        <Property name="LOG_DIR">${sys:user.dir}/logs</Property>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%t] %c{1.} requestId=%X{requestId} - %msg%n</Property>
    </Properties>

    <Appenders>

        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}" />
        </Console>

        <RollingFile
                name="Arquivo"
                fileName="${LOG_DIR}/${APP_NAME}.log"
                filePattern="${LOG_DIR}/${APP_NAME}-%d{yyyy-MM-dd}-%i.log.gz">

            <PatternLayout pattern="${LOG_PATTERN}" />

            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true" />
                <SizeBasedTriggeringPolicy size="10MB" />
            </Policies>

            <DefaultRolloverStrategy max="14" />

        </RollingFile>

    </Appenders>

    <Loggers>

        <Logger name="br.com.jardel.desafio_hotel" level="INFO" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="Arquivo" />
        </Logger>

        <Logger name="org.springframework" level="WARN" />
        <Logger name="org.hibernate" level="WARN" />

        <Root level="INFO">
            <AppenderRef ref="Console" />
            <AppenderRef ref="Arquivo" />
        </Root>

    </Loggers>

</Configuration>
